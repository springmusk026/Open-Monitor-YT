# Required
DATABASE_URL=postgresql://postgres:password@localhost:5432/openmonitoryt
REDIS_URL=redis://localhost:6379

# Optional: seed admin on first run
SEED_ADMIN_EMAIL=

# All other config (LLM, Firecrawl, billing, etc.)
# is stored in the AppConfig table and managed from /dashboard/admin
# These env vars are only needed for bootstrapping.
